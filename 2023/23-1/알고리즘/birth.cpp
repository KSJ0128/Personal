#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
    /*
    multiset<int> unordered;
    multiset<int>::iterator iter;
    int birth;

    for(int i = 0; i < 100; i++)
    {
        cin >> birth;
        unordered.insert(birth);
    }

    for(iter = unordered.begin(); iter != unordered.end(); iter++)
        cout << *iter << " ";
    */

   /*
   int sorted[100];

   for(int i = 0; i < 100; i++)
        cin >> sorted[i];
    
    sort(sorted, sorted + 100);

    for(int i = 0; i < 100; i++)
        cout << sorted[i] << " ";
    */
}