/*
bool 유형: 결과 값으로 true(=1) , false(=0) 값을 취한다.
bool 변수에 숫자를 입력해도 되는데 값이 0이 아닌 경우는 모두 true고, 0인 경우는 false다.
!(0이 아닌 숫자) == 0

if (부울식)
{
    실행문
} // if문 형태

if문 작성시 조건(부울식)에 괄호 필수다.
if문 부울식 뒤에 ; 붙이면 안됨.
부울식에 = 아니고 == 사용.
부울식 true, false 검사시 (변수 == true) 보다 (변수) 형태가 더 나은 표현.
다중 if-else문 작성시 else if문을 줄바꿈 하지 않는 것이 더 좋다.
else는 항상 마지막 if문과 짝을 이룬다.
*/
